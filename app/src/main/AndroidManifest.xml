<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- package removed; namespace is now defined in build.gradle -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LostModeClient">

        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Added android:exported -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
            android:name=".AriaDeviceAdminReceiver"
            android:exported="true"> <!-- Added android:exported -->
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

        <!-- Other components like services -->
        <service
            android:name=".LostModeService"
            android:foregroundServiceType="location"
            android:exported="false" />
            
    </application>
</manifest>
